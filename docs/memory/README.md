# Project memory

These are project notes maintained by Claude Code (its persistent "memory" for
this repo), exported here so they live in version control alongside the code.
They capture non-obvious context — the stack, the admin auth model, and the
security hardening history — that isn't easy to derive from the code alone.

- [`MEMORY.md`](MEMORY.md) — index of the notes below
- [`reef-project-overview.md`](reef-project-overview.md) — what reef.dev is (Vite static site + Vercel serverless API)
- [`reef-admin-auth.md`](reef-admin-auth.md) — how admin authentication works (server-side login, tokens, backup password)
- [`reef-security-hardening.md`](reef-security-hardening.md) — the security audit findings and the fixes applied

This is a point-in-time snapshot; the live memory may evolve in later sessions.
